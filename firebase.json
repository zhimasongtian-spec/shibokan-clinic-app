{
  "functions": [
    {
      "source": "firebase-functions",
      "codebase": "default",
      "ignore": ["node_modules", ".git", "firebase-debug.log", "firebase-debug.*.log"]
    }
  ],
  "firestore": [
    { "database": "(default)", "rules": "firestore.rules" },
    { "database": "mapping", "rules": "firestore-mapping.rules" }
  ]
}
